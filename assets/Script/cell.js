cc.Class({
    extends: cc.Component,

    properties: {
        u: 0,
        v:0,
        w: 0,
    },

    // use this for initialization
    onLoad: function () {
    },

    // called every frame
    update: function (dt) {

    },
});
